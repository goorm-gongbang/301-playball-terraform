# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
.terraform.lock.hcl
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Secrets (use .example files for templates)
terraform.tfvars
*.auto.tfvars

# Environment config files (contain sensitive values - use config.yaml.example instead)
config.yaml

# Lambda build artifacts
*.zip

# OS
.DS_Store
tfplan
